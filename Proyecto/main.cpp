#include <iostream>

using namespace std;
int main() {
    cout << "Hello, World!" << std;
    return 0;
}

void Sobres(){
    // Pedir al usuario la cantidad de sobres que va a necesitar. Un sobre se define como la cantidad de dinero que se
    // va a destinar en diversos gastos mayoritarios.
};

void Ahorros{
    // En este acceso el usuario va a crear hasta 5 ahorros que se van a deducir directamente de su tarjeta
    // Estos ahorros se deben calcular mediante *meta de ahorro*/ *el tiempo en el que se espera ahorrar el dinero*
    // Se debe preguntar al usuario si quiere aportar dinero automaticamente cada semana, quincena o mes
    // En caso de que el usuario no tenga dinero suficiente el ahorro no quita dinero de la cuenta principal
};

void Inversiones{
    // En este acceso el usuario va a esoger hasta 7 partes para invertir. Estas pueden ser lo que sea y queda a libertad
    // del usuario escogerla. Las inversiones van a funcionar de forma muy similar a los ahorros
};